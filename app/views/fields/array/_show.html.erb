<%#
# Array Show Partial

This partial renders an array attribute,
to be displayed on a resource's show page.

By default, the attribute is rendered as a HTML list

## Local variables:

- `field`:
  An instance of Administrate::Field::Array
  A wrapper around the array pulled from the database.
%>

<ul>
  <% field.data.each do |item| %>
    <li><%= item %>
  <% end %>
</ul>
