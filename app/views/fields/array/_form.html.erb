<%#
# Image Form Partial

This partial renders an input element for an array
By default, the input is a text field which will be split on spaces

## Local variables:

- `f`:
  A Rails form generator, used to help create the appropriate input fields.
- `field`:
  An instance of Administrate::Field::Array
  A wrapper around the array pulled from the db
%>

<div class="field-unit__label">
  <%= f.label field.attribute %>
</div>
<div class="field-unit__field">
  <%= f.text_field field.attribute&.join(' ') %>
</div>
